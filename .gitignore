# Python virtualenvs, bytecode, and packaging metadata.
.venv/
__pycache__/
*.py[cod]
*.egg-info/
*.tsbuildinfo
.pytest_cache/
.mypy_cache/
.ruff_cache/

# Frontend build output and dependencies.
node_modules/
.next/
.turbo/
.npm/

# Secrets stay local; only the example is committed.
.env
*.env
!.env.example

# Local design-tool state and generated visual-audit output.
.impeccable/

# Test, benchmark, coverage, and runtime output.
.coverage
.coverage.*
coverage.xml
htmlcov/
*.log
*.sqlite3
data/benchmarks/
data/reports/
data/voice_benchmark/*.json

# `data/` inputs vs. generated artifacts (see data/README.md).
# Tracked exceptions: data/voice_benchmark/*.wav|*.pcm fixtures and
# data/README.md itself. Everything below is rebuilt via make targets.
.cache/
hf_cache/
models--*/
data/raw/
data/cleaned/
data/index/
data/chunks/
data/manuals/
data/models/
data/assets/
tmp/

# OS noise.
.DS_Store
